<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.englishlearn">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".UpdateActivity"
            android:launchMode="singleTask" />
        <activity android:name=".UpdateActivity1" />
        <activity android:name=".List1Activity" />
        <activity android:name=".ListDetailActivity" />
        <activity android:name=".PasswordEditActivity" />
        <activity android:name=".activity.WordLearnActivity" />
        <activity android:name=".activity.ReciteActivity" />
        <activity android:name=".activity.WordListenActivity" />
        <activity android:name=".activity.BookListActivity" />
        <activity android:name=".activity.BookDetailActivity" />
        <activity android:name=".activity.VideoPlayActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>